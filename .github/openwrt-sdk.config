# Package-only SDK build: do not select the complete kernel-module set.
# telEgo contains only user-space packages and has no kmod dependencies.
# This fragment is merged by Kconfig during `make defconfig` in gh-action-sdk.
CONFIG_ALL_KMODS=n
